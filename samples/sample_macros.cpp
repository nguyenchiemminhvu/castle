#include "sample_support.h"

#include "castle/utility/macros.h"

// Scenario: compile-time utility macro integration test.
int main()
{
    // Macro helpers are preprocessor facilities; compile inclusion verifies the
    // target sees the same portable definitions as the library headers.
    return 0;
}

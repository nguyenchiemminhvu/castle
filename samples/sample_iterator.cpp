/**
 * Castle 2.0 sample: <castle/iterator/iterator.h>
 *
 * This header is the iterator umbrella/base include. Its useful behavior is
 * exposed by the concrete fixed/circular/reverse iterator headers, which have
 * dedicated samples in this directory. Keeping this translation unit standalone
 * verifies that the umbrella remains independently includable.
 */
#include "sample_support.h"
#include "castle/iterator/iterator.h"

int main()
{
    return 0;
}
